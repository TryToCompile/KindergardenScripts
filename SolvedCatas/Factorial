using System;

public static class Kata
{
  public static int Factorial(int n)
  { 
     Exception ex = null;
     if (n < 0) {
      ex = new Exception();
      throw ex; 
     }
     if (n == 0) return 1;
     if (n == 1) return n;
     return n*Factorial(n-1);
  }
}
